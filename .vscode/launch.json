{
  "configurations": [
    {
      "name": "Docker: Python",
      "type": "python",
      "request": "attach",
      "preLaunchTask": "wait",
      "connect": {
        "host": "localhost",
        "port": 5678
      },
      "pathMappings": [
        {
          "localRoot": "${workspaceFolder}",
          "remoteRoot": "."
        }
      ]
    }
  ]
}
